import { Box } from '@mui/material';
import React from 'react'
import { GoogleButton } from '../components/GoogleButton';

export const Auth = () => {
  return (
    <Box>
        <GoogleButton/>
    </Box>
  )
}

export default Auth;